<% if(user.badge == 'Etudiants') { %>
    <div class="nav-etu">
      <span>MADA-UNIVERSITY</span>
        <ul>
          <li><a href="/changerMdp/<%= user.id %>" class="btn">Changer mot de passe</a></li>
      
          <li>
            <% if(user){ %>
            <a href="/logout" class="btn">Deconnexion</a>
            <% } %>
          </li>
        </ul>
      </div>
      <div class="container-fluid">
        <div class="row">
          <div class="col-lg-2 etu-menu">
            <div class="form-group  icone">
              <i class="fa fa-info"></i> <a href="/etudiants/home">Informations </a>
            </div>
            <div class="form-group icone">
              <i class="fa fa-calendar"></i>  <a href="/etudiants/edt/<%= etudiant.FiliereId %>/<%= etudiant.OptionId %>/<%= etudiant.SemestreId %>">Emploi du temps</a>
            </div>
            <div class="form-group icone">
                <% if(etudiant.delegue == true){ %>
                <i class="fa fa-book"></i>  <a href="/etudiants/cahierTexte">Cahier de texte</a>
                <% } %> 
            </div>
            <div class="form-group icone">
               <i class="fa fa-sticky-note"></i> Releve de Note : 
                <ul>
                  <% for(item of semestres){ %>
                  <li><a href="/etudiants/releveNote/<%= etudiant.FiliereId %>/<%= etudiant.OptionId %>/<%= item.id %>/<%= etudiant.id %>">S<%= item.nom %></a> </li>
                  <% } %>
                </ul>
              
            </div>
      
          </div>
          <div class="col-lg-7 changerMdp">
            <h3>Changer Mot de passe</h3>
            <form action="" method="post">
                <% if(error){ %>
                    <% for(item of error){ %>
                        <div class="alert alert-danger">
                            <%= item %>
                        </div>
                    <% } %>
                <% } %>
                <div class="form-group">
                    <input type="hidden" name="userId" value="<%= user.id %>">
                    <label for="">Ancien Mot de Passe : </label>
                    <input type="password" name="ancienMotDePasse" placeholder="" class="form-control">    
                </div>
                <div class="form-group">
                    <label for="">Nouveau Mot de passe : </label>
                    <input type="password" name="password" placeholder="" class="form-control">
                </div>
                <div class="form-group">
                    <label for="">Confirmation du nouveau mot de passe : </label>
                    <input type="password" name="confirmation" placeholder="" class="form-control">

                </div>
                <div class="form-group">
                  <button type="submit" class="btn form-control">Confirmer</button>
             </div>
               
            </form>
               
            </div>
            
        </div>

    </div>
<% }else if (user.badge == 'Profs') { %>

    <div class="nav-etu">
      <span>MADA-UNIVERSITY</span>
        <ul>
          <li><a href="/changerMdp/<%= user.id %>" class="btn">Changer mot de passe</a></li>
      
          <li>
            <% if(user){ %>
            <a href="/logout" class="btn">Deconnexion</a>
            <% } %>
          </li>
        </ul>
      </div>
    
    
      <div class="container-fluid">
        <div class="row">
          <div class="col-lg-2 etu-menu">
            <div class="form-group  icone">
              <i class="fa fa-info"></i> <a href="/profs/home">Informations </a>
            </div>
            <div class="form-group icone">
              <i class="fa fa-calendar"></i> <a href="/profs/edt/<%= prof.FiliereId %>/<%= prof.OptionId %>/<%= prof.SemestreId %>">Emploi du temps</a>
            </div>
            <div class="form-group icone">
                <i class="fa fa-book"></i>  <a href="/profs/cahierTexte">Cahier de texte</a>
            </div>
          </div>
          <div class="col-lg-7 changerMdp" >
            <h3>Changer Mot de passe</h3>
            <form action="" method="post" >
                <% if(error){ %>
                    <% for(item of error){ %>
                        <div class="alert alert-danger">
                            <%= item %>
                        </div>
                    <% } %>
                <% } %>
                <div class="form-group">
                    <input type="hidden" name="userId" value="<%= user.id %>">
                    <label for="">Ancien Mot de Passe : </label>
                    <input type="password" name="ancienMotDePasse" placeholder="" class="form-control">    
                </div>
                <div class="form-group">
                    <label for="">Nouveau Mot de passe : </label>
                    <input type="password" name="password" placeholder="" class="form-control">
                </div>
                <div class="form-group">
                    <label for="">Confirmation du nouveau mot de passe : </label>
                    <input type="password" name="confirmation" placeholder="" class="form-control">

                </div>
                <div class="form-group">
                     <button type="submit" class="btn form-control">Confirmer</button>
                </div>
               
            </form>
               
            </div>
        </div>
      </div>
    
    
<% } %>
